struct A;

trait Trait {
    fn foo() -> Self;

    fn goo(&self) -> &Self {
        self
    }
}

impl Trait for A {
    fn foo() -> Self {
        A
    }

    fn goo(&self) -> &Self {
        self
    }
}

fn main() {
    let a: A = A::foo();
    let b: &A = a.goo();

    exit(0);
}
