fn main() {
    let mut i: i32 = 100;
    let mut total: i32 = 0;
    while (i > 0) {
        let mut j: i32 = i;
        while (j > 0) {
            total += j;
            j -= 1;
        }
        i -= 1;
    }
    printlnInt(total);
    exit(0);
}